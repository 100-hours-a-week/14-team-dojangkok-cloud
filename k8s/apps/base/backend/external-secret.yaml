apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: backend-secret
  namespace: dojangkok
  labels:
    app.kubernetes.io/name: backend
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: backend-secret
  data:
    - secretKey: application.yaml
      remoteRef:
        key: dojangkok/dev/backend
